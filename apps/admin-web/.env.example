# URL base de la API del panel municipal.
# Ruta relativa a proposito: vite.config.ts proxea /api a http://localhost:3001
# (backend-admin), asi el panel y su API quedan bajo un mismo origen.
VITE_API_URL=/api
