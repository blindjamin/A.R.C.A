fix(admin): corregir asignación de operadores (UUIDs reales)

Se creó una migración para sembrar en la BD los 4 operadores de prueba
que el panel muestra en el dropdown, de modo que sus IDs sean UUIDs válidos.
El frontend fue actualizado para usar estos UUIDs al hacer el PATCH,
evitando el error 400 del backend.

IA: agente
HU: ninguna
Revisor: pendiente
